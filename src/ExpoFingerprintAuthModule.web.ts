import { registerWebModule, NativeModule } from 'expo';

class ExpoFingerprintAuthModule extends NativeModule {
}

export default registerWebModule(ExpoFingerprintAuthModule, 'ExpoFingerprintAuthModule');
